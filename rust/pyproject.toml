[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "pagb-kernels"
version = "0.1.0"
description = "Compiled Rust kernels for PAGB reconstruction"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
